[project]
name = "signac-deps"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "signac>=2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]
